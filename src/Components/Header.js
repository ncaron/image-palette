import React from 'react';
import Logo from './Logo';

const Header = () => {
  return (
    <header className="header">
      <Logo />
      <h1>Image Palette</h1>
    </header>
  );
};

export default Header;
